import React from 'react'
import { Container } from './styles'

export default function SidebarHeaderButton({onClick}) {
  return (
    <Container onClick={onClick}>
        <span />
        <span />
        <span />
    </Container>
  )
}
